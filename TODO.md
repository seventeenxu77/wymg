# 藏金匿影：开发路线与 TODO

> 最后更新：2026-07-31
> 这份文件是后续开发的任务入口。开始新一轮工作前先阅读本文件；完成任务后同步更新勾选状态、验收结果和“下一步”。

## 已确认的开发原则

- 经典本地双人模式保持独立，继续支持同键盘、双分屏和四局身份轮换。
- 联机、单人冒险和回合战棋从独立模式入口进入，不在经典模式里堆叠网络分支。
- `1v3` 不考虑同键盘操作，必须通过联机实现。
- 联机开发优先支持同一台电脑启动一个服务器和多个客户端窗口。
- 新模式前期可以使用占位视觉，但菜单与大厅应复用主菜单的背景、边框、字体和动效语言。
- 联机对局采用服务器权威思路：客户端发送输入，服务器决定角色、房间、道具和胜负状态。
- 新增状态字段先进入统一工厂；长期逐步把动态 `Dictionary` 换成类型化 `Resource` / Class。

## 当前已经完成

- [x] 保留原有“经典本地双人”玩法和输入方式。
- [x] 增加独立模式选择入口。
- [x] 预留“联机狩猎”“单人冒险”“回合战棋”模式。
- [x] 建立 ENet 创建房间、加入房间、玩家槽和连接状态框架。
- [x] 建立本机 `1v3` 多实例启动与停止脚本。
- [x] 联机大厅迁移经典三格背包样式，普通对局每人固定获得一件肾上腺素。
- [x] 联机大厅加入职业占位选择：收藏家、侦察者、支援者和搬运者。
- [x] 模式选择和联机大厅复用主菜单视觉，并完整覆盖游戏背景。
- [x] 在 `800×450` 四窗口调试尺寸下验证菜单与大厅布局。
- [x] 抽离小地图渲染、教程步骤目录和运行状态构造工厂。
- [x] 为模式路由、联机会话和状态工厂补充回归测试。
- [x] 完成可进入的联机对局骨架与四客户端权威移动同步。
- [x] 将现有 2.5D 宅邸、家具、多人碰撞和基础阶段迁入联机模式。

## 里程碑 1：可进入的联机对局骨架（当前最高优先级）

- [x] 新建独立的网络比赛场景，不复用经典模式的双分屏 HUD。
- [x] 每个客户端只创建自己的输入控制器和单个玩家视角。
- [x] 服务器生成并维护房间、玩家、阶段和基础计时状态。
- [x] 客户端上传输入，服务器广播权威快照。
- [x] 为远端角色增加位置插值，避免移动抖动。
- [x] 大厅增加准备状态和房主开始按钮。
- [x] 增加资源加载屏障：所有客户端完成地图和渲染预热后，由服务器统一开场。
- [x] 联机客户端只创建本机阵营的单个 3D 视口，并裁掉另一阵营的高墙与幽灵远景副本。
- [x] 缓存地板、常用标准材质和动态纹理；墙体改用共享材质与实例光照／淡入参数。
- [x] 大厅增加职业占位选择，并由服务器校验阵营、在准备后锁定。
- [x] 大厅显示带图标的肾上腺素和两个空装备槽，不提前免费开放九种道具。
- [x] 固定四个联机槽位：`monster`、`thief-1`、`thief-2`、`thief-3`。
- [x] 支持玩家离开后的槽位释放。
- [ ] 显示连接失败、房间已满等完整错误。
- [x] 从大厅开始游戏后，四个窗口进入同一张占位地图并能互相看到移动。
- [x] 增加网络对局最小自动化测试。

验收标准：运行 `.\dev\run_1v3.ps1` 后，一个怪物客户端和三个盗贼客户端能自动进入同一大厅；房主开始后，四个窗口进入同一局并同步基础移动。此阶段不要求正式图片、战斗或完整胜负。

## 里程碑 1.5：现有本地内容迁移到联机

- [x] 复用现有随机 36 房间宅邸生成。
- [x] 复用现有地板、墙体、家具、角色纸片和房间光照。
- [x] 支持一个怪物和三个盗贼在同一房间正确显示。
- [x] 复用经典模式的墙体、门洞、角色半径和家具占地碰撞。
- [x] 每个客户端使用单视角摄像机，并支持 `Q / E` 旋转。
- [x] 同步藏宝、准备倒计时和狩猎阶段；怪物可按 `H` 提前结束藏宝。
- [x] 显示联机玩家列表、阶段计时和 6×6 小地图。
- [x] 迁移怪物打开家具、放置三件专属藏品的流程。
- [x] 迁移盗贼撞击／损毁家具和物品掉落。
- [x] 迁移物品拾取、个人携带与入口撤离交互，但暂不决定胜负。
- [x] 迁移噪音、盗贼静止隐身和同房可见规则。
- [x] 联机噪音在角色中心按屏幕左／右显示扩散波纹，小地图仅作为辅助提示。
- [x] 怪物视角不显示盗贼实体，移动盗贼改用本地版红色残影表现。
- [x] 迁移怪物攻击、盗贼生命、受击硬直和倒地状态。
- [x] 实现服务器权威的持续救援、中断、复起生命和短暂无敌。
- [x] 迁移现有道具与部署装置。
  - [x] 第一批：肾上腺素、替身玩偶、留声机和捕兽夹。
  - [x] 联机装备栏、选择／使用请求、装置快照、触发事件和捕兽夹回收。
  - [x] 第二批：探测器、警报器、传送器、弹簧拳套和发条巡夜偶。
  - [x] 探测器电量／房间标记、家具全局警报、传送蓄力与受击中断。
  - [x] 弹簧拳套击退／眩晕、巡夜偶巡逻／警戒／停机／换位和紧凑快照。
- [x] 大厅初始背包随玩家快照进入比赛，角色生成时继承一件肾上腺素。
- [ ] 迁移商店、装备栏、四局身份轮换和经济系统。
- [ ] 最后单独讨论并实现 `1v3` 胜负与结算规则。

## 里程碑 2：职业与技能系统

- [x] 怪物与盗贼使用独立职业池；怪物首个职业为收藏家。
- [ ] 确定首批职业数量以及每个职业的定位、数值预算和克制关系。
- [x] 建立类型化 `ProfessionDefinition` 数据资源与职业目录。
- [ ] 建立独立于道具栏的 `SkillDefinition` 与技能运行系统。
- [ ] 支持主动技能、被动技能、冷却、消耗、施法范围和网络同步。
- [x] 在赛前大厅加入职业选择、准备锁定和服务器阵营校验。
- [ ] 决定盗贼职业是否允许重复选择，并按规则增加服务器限制。
- [ ] HUD 增加技能栏、冷却提示和关键状态提示。
- [x] 加入收藏家、侦察者、支援者和搬运者四个占位职业，暂不改变属性。
- [ ] 为每个首发职业制作最小可玩的技能组合。

待讨论的关键问题：

- 每个职业采用“一个主动技能 + 一个被动技能”，还是更多技能？
- 怪物是否唯一且更强，三名盗贼是否必须承担侦察、支援、搬运等互补职责？
- 职业可否重复选择？
- 技能是在每局固定，还是能够在局间商店升级或替换？

## 里程碑 3：可设计地图与功能房间

- [ ] 将地图拓扑、房间定义和视觉场景从当前生成逻辑中分离。
- [ ] 建立可由设计人员配置的地图资源或场景格式。
- [ ] 支持手工摆放房间、门、入口、撤离点、家具和出生点。
- [ ] 增加地图连通性、出生距离、死路和关键目标可达性检查。
- [ ] 建立类型化 `RoomDefinition`，替换仅靠动态字段区分房间的方式。
- [ ] 首批设计至少三种功能房间，并让功能真正影响玩法。
- [ ] 为房间配置独立材质、灯光、音效和小地图标记。
- [ ] 决定最终地图是完全手工设计，还是“手工房间模块 + 受控随机拼装”。

候选功能房间：

- 金库／收藏室：高价值目标集中，但容易触发警报。
- 监控室：可查看或暂时干扰部分房间信息。
- 工坊：维修、强化或补充有限装置。
- 密道房：提供风险较高的跨区移动。
- 配电室：改变照明、门锁或部分装置状态。

## 里程碑 4：完整 `1v3` 实时狩猎

- [ ] 将藏宝、搜刮、追踪、攻击和撤离规则适配一名怪物对三名盗贼。
- [ ] 明确盗贼共享目标、个人收益、倒地、救援和淘汰规则。
- [ ] 处理三名盗贼同时交互同一家具或物品时的服务器裁决。
- [ ] 重做信息与噪音系统，避免怪物因目标过多而无法判断。
- [ ] 调整房间规模、计时、价值、生命和技能冷却。
- [ ] 加入观战、掉线处理和比赛结束流程。
- [ ] 完成至少一张专门为 `1v3` 设计的地图。

## 里程碑 5：其他模式

### 单人 AI 冒险

- [ ] 明确玩家扮演怪物、盗贼，还是两者均可选择。
- [ ] 建立 AI 感知、寻路、目标选择和行为树／状态机。
- [ ] 让 AI 使用职业技能、道具、房间功能和撤离规则。
- [ ] 支持玩家与 AI 队友共同冒险。
- [ ] 设计关卡目标、难度曲线、失败恢复和长期成长。

### 回合制战棋

- [ ] 明确其与实时狩猎共享的数据：职业、技能、房间和世界观。
- [ ] 建立独立回合流程、行动点、格子／节点移动和结算场景。
- [ ] 不直接修改经典实时模式的移动与输入系统。

### 其他即时模式

- [ ] 在联机狩猎稳定后再确定规则，优先复用网络比赛基础设施。

## 持续维护任务

- [ ] 继续拆分 `game_hud.gd` 中的主菜单、局内 HUD、商店和结算页面。
- [ ] 将部署装置的生命周期从 `tool_system.gd` 拆到独立系统。
- [ ] 继续隔离地图渲染与地图数据。
- [ ] 继续拆分教程流程、教程 UI 和教程 AI。
- [ ] 将角色、房间、装置、状态效果逐类迁移为类型化数据。
- [ ] 新功能必须补充对应测试，避免经典本地模式回归。
- [ ] 每个可玩里程碑都验证 `1600×900` 正常窗口和 `800×450` 多实例窗口。

## 下一步

下一项建议讨论收藏家、侦察者、支援者和搬运者的首批主动／被动技能，确定技能槽数量、
冷却与资源模型、盗贼职业能否重复，以及各职业的基本数值预算。规则确认后先建立类型化
`SkillDefinition` 和服务器权威技能请求，不把技能状态混入道具栏。带金币预算的商店、四局
身份轮换和经济结算仍不直接照搬经典模式，需要与最终胜负、收益分配一起单独讨论。

## 常用验证命令

```powershell
# 启动一个无窗口服务器和四个客户端
.\dev\run_1v3.ps1

# 启动后让四个客户端自动向不同方向移动
.\dev\run_1v3.ps1 -AutoMove

# 跳过藏宝但保留手动移动，方便测试潜行、噪音和战斗
.\dev\run_1v3.ps1 -SkipHide

# 近距离生成四名角色，直接测试攻击、倒地和救援
.\dev\run_1v3.ps1 -CombatTest

# 近距离生成并分配全部九种测试道具
.\dev\run_1v3.ps1 -ToolTest

# 四窗口停留在大厅，检查三格背包后逐个准备
.\dev\run_1v3.ps1 -ManualLobby

# 关闭上述实例
.\dev\stop_1v3.ps1

# 运行单个网络框架测试
godot --headless --path . --script res://tests/network_mode_framework.gd

# 运行第二批联机道具规则与快照测试
godot --headless --path . --script res://tests/network_tools_second_batch.gd
```
